# Build Journal -- 2026-02-14

## Session: Vault Code Review Fix Sweep
**Started**: 2026-02-14
**Source**: Qwen 14B review of 234 files (`06-CLAUDE-LOGS/2026-02-13-vault-sweep-review.md`)

### Scope
- 13 files flagged by Qwen as having potential bugs
- ~225 files skipped (description-only, generic advice, empty inits, >50K)

### Results
- **1 fix applied**: Security -- removed `i_secret` from alert payload in `atr_position_manager_v1.pine`
- **12 false positives**: Guards already existed, code was correct, or claims were wrong
- **92% false positive rate** from Qwen's review

### Fix Applied
- `indicators/supporting/atr_position_manager_v1.pine`: Removed webhook secret from alert JSON body. Added comment recommending URL-based auth or IP allowlist.

### Key False Positives
- `exit_manager.py`: `entry + offset` for LONG BE is CORRECT (covers fees above entry)
- `position_v383.py` + `v384.py`: `!= 0` early-return guard is CORRECT (Qwen misread control flow)
- `four_pillars_v3.pine` + `v3_8_2.pine`: Zero guards already exist in stoch calculations
- `bet_sizing.py`: `avg_loss >= 0` guard already catches zero case
- `aggregator.py`: `min(idx, len-1)` bounds check already exists

### Sweep 2 Results (vault_sweep_4.py -- 62 files)
- **42 RED entries**: 0 actionable bugs
- 6 description-only, 3 skipped >50K, 25 generic try/except, 8 specific claims all false
- Notable false positives: Qwen said LONG SL should be ABOVE entry (would break trading), said TR[0] should be NaN (would break ATR), said `> 1` differs from `>= 2` (identical for ints)
- Rated v383 state machine GREEN but v382 RED for the same `>` operator

### Combined Stats (2 sweeps, ~300 files)
- 1 fix applied total (webhook secret)
- ~96% false positive rate on specific claims
- Qwen useful for finding files to check, not for the actual analysis

### Change Log
Full details: `06-CLAUDE-LOGS/2026-02-14-vault-sweep-fixes.md`

---

## Session: Dashboard v3.2 -- Bugfixes + UX from User Testing
**Started**: 2026-02-14 16:30 UTC

### Scope
Patched `scripts/dashboard.py` (1893 -> 1895 lines) with 24 patches via `scripts/build_dashboard_v32.py`. Fixed 12 categories of bugs found during live user testing of v3.1.

### Pre-work: Full Audit
Read all 1893 lines of dashboard.py line-by-line. Found 14 total issues (12 code patches, 2 non-code items).

### Patches Applied (24 total, 12 categories)

| # | Patch | Type |
|---|-------|------|
| P1 | Clip portfolio DD at -100% (np.clip) | Logic fix |
| P2 | Stress test table: 6 columns raw floats + Styler format | Arrow crash fix |
| P3 | Removed `use_container_width=True` from 8 st.dataframe calls | Deprecation fix |
| P4 | Moved buttons block (17 lines) to top of sidebar | UX |
| P5 | Added st.spinner to portfolio backtest | UX |
| P6 | Capped per-coin DD% at -100 in LSG table | Display fix |
| P7 | Added 12 help= tooltips to key metrics | UX |
| P8 | ML Filtered table: raw numeric values | Arrow crash fix |
| P9 | Grades table: 3 columns raw floats + Styler format | Arrow risk fix |
| P10 | TP Impact table: raw numeric values | Arrow risk fix |
| P11 | Added "df": df to sweep_detail_data | Missing data fix |
| P12 | Equity curve x-axis: datetime instead of bar index (4 traces) | Visualization fix |

### Non-code Items
- SHIB: Not in cache. User runs `download_all_available.py` to add 1000SHIBUSDT.
- BTC zigzag: Expected behavior -- commission eats 26.7% of TP on high-price coins.
- Portfolio tabs: Deferred to v3.3 (needs coin selector dropdown).

### Test Results
- `test_dashboard_v32.py`: 23/23 PASS
- `build_dashboard_v32.py --dry-run`: 15/15 anchors, 0 missing
- `build_dashboard_v32.py`: APPLIED, py_compile PASS

### Files

| File | Action | Lines |
|------|--------|-------|
| scripts/dashboard.py | EDITED (24 patches) | 1893 -> 1895 |
| scripts/build_dashboard_v32.py | CREATED | ~290 |
| scripts/test_dashboard_v32.py | CREATED | ~220 |

### Run Command
```
streamlit run "C:\Users\User\Documents\Obsidian Vault\PROJECTS\four-pillars-backtester\scripts\dashboard.py"
```

---

## TODO: Cache Gap Finder (after dashboard verified)
**Priority**: After v3.2 is fully tested in browser
**Problem**: `download_all_available.py` only iterates `fetcher.list_cached()` -- coins never fetched initially (like 1000SHIBUSDT) are invisible forever. 399 cached vs ~485 active Bybit USDT perps = ~86 missing coins.
**Script**: `scripts/find_missing_coins.py`
**Approach**:
1. Fetch ALL active Bybit USDT perps via `/v5/market/instruments-info`
2. Compare against local cache (399 symbols)
3. Cross-reference with CoinGecko market caps (top 2000, $100M+ filter)
4. Report missing high-cap coins, optional `--fetch` flag to download
5. User wants logic for coin selection: cross-check 2017 coin list vs 2023 existence to find long-lived coins
**Reuse**: `fetch_sub_1b.py` has `get_bybit_symbols()`, `get_coingecko_market_caps()`, `strip_bybit_prefix()`
